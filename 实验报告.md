# 基于 FixMatch 的半监督图像分类实验报告

> **课程**: 模式识别 — 第三次作业
> **数据集**: CIFAR-10
> **模型**: WideResNet-28-2 + FixMatch
> **日期**: 2026年6月

---

## 目录

1. [实验目的](#1-实验目的)
2. [FixMatch 算法原理与实现](#2-fixmatch-算法原理与实现)
3. [实验设置](#3-实验设置)
4. [实验结果与分析](#4-实验结果与分析)
5. [与 USB 库实现的对比](#5-与-usb-库实现的对比)
6. [FixMatch 与其他半监督算法的比较](#6-fixmatch-与其他半监督算法的比较)
7. [结论](#7-结论)
8. [参考文献](#8-参考文献)

---

## 1. 实验目的

1. 掌握 **FixMatch 算法**的核心原理——伪标签与一致性正则化的结合。
2. 基于 **PyTorch** 手动实现 FixMatch 半监督图像分类算法，在 **CIFAR-10** 数据集上分别使用 **40、250、4000** 张标注数据进行实验，评估不同标注量下的分类性能。
3. 对比自己实现的算法与 **USB（原 TorchSSL）库**中 FixMatch 实现的效果差异，分析性能差距的原因。
4. 分析 FixMatch 与其他半监督算法（如 **MixMatch**）的异同，理解半监督学习方法论的发展脉络。

---

## 2. FixMatch 算法原理与实现

### 2.1 算法背景

半监督学习（Semi-Supervised Learning, SSL）旨在同时利用少量标注数据 $\mathcal{X} = \{(x_b, y_b)\}_{b=1}^{B}$ 和大量无标注数据 $\mathcal{U} = \{u_b\}_{b=1}^{\mu B}$ 来训练深度学习模型。FixMatch是该领域的重要里程碑，其核心思想极为简洁——**利用模型对弱增强图像的预测作为伪标签，监督同一图像强增强版本的输出**。

### 2.2 损失函数

FixMatch 的总损失由两部分组成：

$$
\mathcal{L} = \mathcal{L}_s + \lambda_u \cdot \mathcal{L}_u
$$

#### 2.2.1 有监督损失 $\mathcal{L}_s$

对有标签数据使用标准的交叉熵损失：

$$
\mathcal{L}_s = \frac{1}{B}\sum_{b=1}^{B} H(y_b, p_{\text{model}}(y \mid \alpha(x_b)))
$$

其中 $\alpha(\cdot)$ 为弱增强（RandomHorizontalFlip + RandomCrop），$H(\cdot,\cdot)$ 为交叉熵函数。

#### 2.2.2 无监督损失 $\mathcal{L}_u$

无监督损失是 FixMatch 的核心创新点，分三步完成：

**Step 1 — 伪标签生成**：对无标签图像施加**弱增强** $\alpha(\cdot)$，用模型预测其类别分布：

$$
q_b = p_{\text{model}}(y \mid \alpha(u_b))
$$

取最大概率类别作为伪标签 $\hat{y}_b = \arg\max(q_b)$。

**Step 2 — 置信度过滤**：仅保留预测置信度高于阈值 $\tau$ 的伪标签。这是 FixMatch 区别于早期伪标签方法的关键——低置信度的伪标签往往是噪声，保留它们会损害训练：

$$
\text{mask}_b = \mathbb{1}[\max(q_b) \geq \tau]
$$

本实验中 $\tau = 0.95$，意味着仅约 5%–50% 的无标签样本参与训练（比例随训练进程增长）。

**Step 3 — 一致性正则化**：对同一无标签图像施加**强增强** $\mathcal{A}(\cdot)$（RandAugment + RandomErasing），用 Step 1 生成的伪标签监督强增强版本的输出：

$$
\mathcal{L}_u = \frac{1}{\mu B}\sum_{b=1}^{\mu B} \text{mask}_b \cdot H(\hat{y}_b, p_{\text{model}}(y \mid \mathcal{A}(u_b)))
$$

强增强迫使模型学习更具鲁棒性的特征表示，而只有高置信度伪标签参与训练则保证了无监督信号的可靠性。"弱增强生成伪标签 + 强增强一致性正则化"的设计，使 FixMatch 以极简的形式达到了当时最优的半监督学习性能。

### 2.3 核心模块的代码实现

根据实验要求，以下核心模块为手动编写：

#### 模块 1：伪标签生成与阈值过滤（`train.py` 第 264–271 行）

```python
# 1. 用弱增强图像生成伪标签（无梯度计算）
with torch.no_grad():
    logits_u_w = model(inputs_u_w)
    max_probs, pseudo_labels = torch.max(
        torch.softmax(logits_u_w, dim=1), dim=1
    )
    # 2. 置信度阈值过滤：仅保留 prob >= 0.95 的样本
    mask = max_probs.ge(args.threshold).float()

# 3. 用伪标签监督强增强版本的输出
logits_u_s = model(inputs_u_s)
loss_u = (F.cross_entropy(logits_u_s, pseudo_labels, reduction='none') * mask).mean()
```

**设计要点**：

- 伪标签生成时不计算梯度（`torch.no_grad()`），防止无监督信号通过伪标签分支反向传播导致模型坍缩。
- 使用 `reduction='none'` 逐样本计算损失后乘以 mask，实现对低置信度样本的精确排除。
- `mask.float().mean()` 可监控伪标签利用率，反映模型对无标签数据的信心。

#### 模块 2：强弱数据增强（`utils/augment.py`）

弱增强 $\alpha(\cdot)$（用于伪标签生成和有标签训练）：

```python
transforms.Compose([
    transforms.RandomHorizontalFlip(),
    transforms.RandomCrop(size=32, padding=4, padding_mode='reflect'),
    transforms.ToTensor(),
    transforms.Normalize(mean=CIFAR10_MEAN, std=CIFAR10_STD)
])
```

强增强 $\mathcal{A}(\cdot)$（仅用于无监督一致性正则化）：

```python
transforms.Compose([
    transforms.RandomHorizontalFlip(),
    transforms.RandomCrop(size=32, padding=4, padding_mode='reflect'),
    transforms.RandAugment(num_ops=2, magnitude=10),  # 核心：随机增强
    transforms.ToTensor(),
    transforms.RandomErasing(p=0.5, scale=(0.02, 0.2)),  # 随机擦除
    transforms.Normalize(mean=CIFAR10_MEAN, std=CIFAR10_STD)
])
```

**设计要点**：

- 强/弱增强的差异化是 FixMatch 成功的关键——弱增强保证伪标签的稳定性，强增强迫使模型学习不变性。
- RandAugment 从 14 种增强操作中随机选择 2 种，以固定强度应用，大幅简化了 AutoAugment 的搜索过程。
- 无标签数据的弱增强和强增强版本均来自同一原始图像，仅在增强管道不同，这确保了语义一致性。

#### 模块 3：EMA 指数移动平均（`train.py` 第 24–78 行）

```python
class EMAModel:
    def __init__(self, model, decay=0.999):
        self.shadow = {}   # 存储参数的指数滑动平均值
        self.decay = decay

    def update(self):
        """在每次 optimizer.step() 后调用"""
        for name, param in self.model.named_parameters():
            if param.requires_grad:
                # shadow = decay * shadow + (1 - decay) * param
                self.shadow[name].mul_(self.decay).add_(param.data, alpha=1 - self.decay)
```

**设计要点**：

- EMA 将模型参数的指数滑动平均作为最终推理参数，有效平滑训练过程中的参数波动。
- 衰减率 0.999 意味着当前参数仅贡献 0.1% 的更新，影子参数更接近训练的长期趋势，泛化性能更好。
- 评估时将影子参数临时加载到模型中，评估完成后恢复原始参数继续训练。

#### 模块 4：数据划分策略（`dataset.py`）

```python
def split_labeled_unlabeled(targets, n_labeled_per_class, num_classes=10):
    """按类别均匀采样有标签样本，所有样本均作为无标签数据"""
    for i in range(num_classes):
        idx = np.where(targets == i)[0]
        sampled = np.random.choice(idx, n_labeled_per_class, replace=False)
        labeled_idx.extend(sampled)
    unlabeled_idx = np.array(range(len(targets)))  # 包含所有训练数据
    return labeled_idx, unlabeled_idx
```

**设计要点**：

- 采用**类别均匀采样**策略，确保标注数据覆盖所有 10 个类别。
- 无标签集包含全部训练数据（含已标注的），但标签信息在无监督分支中不可见。
- 使用固定随机种子（seed=42）确保数据划分可复现。

### 2.4 训练策略

| 组件           | 配置                         | 说明                                                          |
| -------------- | ---------------------------- | ------------------------------------------------------------- |
| 学习率调度     | 余弦退火（Cosine Annealing） | $\eta_t = 0.03 \cdot \frac{1}{2}(1 + \cos(\frac{t}{T}\pi))$ |
| 优化器         | SGD + Nesterov 动量          | momentum=0.9, weight_decay=5e-4                               |
| 混合精度       | AMP (FP16)                   | 使用`torch.cuda.amp` 加速训练                               |
| 无标签批量倍数 | $\mu = 7$                  | 无标签 batch = 有标签 batch × 7 = 448                        |
| EMA 衰减率     | 0.999                        | 每次 step 后更新影子参数                                      |

---

## 3. 实验设置

### 3.1 数据集

| 属性   | 说明                                                                 |
| ------ | -------------------------------------------------------------------- |
| 数据集 | CIFAR-10（10类自然彩色图像）                                         |
| 训练集 | 50,000 张 32×32×3 彩色图像                                         |
| 测试集 | 10,000 张 32×32×3 彩色图像                                         |
| 类别   | airplane, automobile, bird, cat, deer, dog, frog, horse, ship, truck |

### 3.2 标注数据配置

| 配置          | 总标注数 | 每类标注数 | 标注占比 | 场景         |
| ------------- | :------: | :--------: | :------: | ------------ |
| FixMatch-40   |    40    |     4     |  0.08%  | 极度稀缺标注 |
| FixMatch-250  |   250   |     25     |  0.50%  | 中等稀缺标注 |
| FixMatch-4000 |   4000   |    400    |  8.00%  | 较充足标注   |

### 3.3 模型与超参数

| 超参数                      | 值                                                    |
| --------------------------- | ----------------------------------------------------- |
| 骨干网络                    | WideResNet-28-2（深度28，宽度因子2，~1.47M参数）      |
| 初始学习率                  | 0.03（余弦退火至0）                                   |
| 优化器                      | SGD + Nesterov (momentum=0.9, weight_decay=5×10⁻⁴) |
| 有标签 Batch Size           | 64                                                    |
| 无标签 Batch Size           | 448（$\mu = 7$）                                    |
| 置信度阈值$\tau$          | 0.95                                                  |
| 无监督损失权重$\lambda_u$ | 1.0                                                   |
| EMA 衰减率                  | 0.999                                                 |
| 每 Epoch 训练步数           | 1024                                                  |
| 总训练 Epoch                | ~256（约 262,144 步）                                 |
| 随机种子                    | 42                                                    |

---

## 4. 实验结果与分析

### 4.1 总体性能对比

![最佳准确率柱状图](data/best_accuracy_bar.png)

**图 1：三种标注配置下的最佳准确率对比**

| 指标                          | FixMatch-40 | FixMatch-250 |  FixMatch-4000  |
| ----------------------------- | :---------: | :----------: | :--------------: |
| 最高测试准确率 (Max Test Acc) |   69.10%   |    91.88%    |      94.49%      |
| 最高 EMA 准确率 (Max EMA Acc) |   69.55%   |    91.79%    | **94.62%** |
| 最终测试准确率 (Final)        |   68.99%   |    91.73%    |      94.46%      |
| 全监督基线 (WRN-28-2)         |     —     |      —      |      ~95.0%      |

**核心发现**：

1. **FixMatch-4000（94.62%）已接近全监督性能（~95.0%）**，仅使用 8% 的标签即几乎消除了与全监督的差距，充分验证了 FixMatch 对无标签数据的有效利用。
2. **FixMatch-250（91.79%）在仅 0.5% 标签下突破 90% 准确率**，证明在极少量标注下模型依然能学到有效的分类决策边界。
3. **FixMatch-40（69.55%）虽远低于全监督，但远超随机猜测和仅用 40 标签做全监督训练**。每类仅 4 张标签时，FixMatch 通过 50,000 张无标签图像提供的无监督信号大幅提升了性能。
4. **标注数据的边际收益递减**：40→250（×6.25）提升 +22.24%；250→4000（×16）仅提升 +2.83%。

### 4.2 训练过程分析

![综合对比](data/full_comparison.png)

**图 2：训练过程中的测试准确率、EMA 准确率与损失函数变化全景**

#### 准确率随 Epoch 变化

![测试准确率曲线](data/accuracy_comparison.png)

**图 3：三种标注配置的测试准确率平滑曲线（窗口=15）**

| 里程碑    | FixMatch-40 | FixMatch-250 | FixMatch-4000 |
| --------- | :---------: | :----------: | :-----------: |
| Epoch 1   |   18.02%   |    48.20%    |    63.20%    |
| Epoch 10  |   26.55%   |    73.07%    |    81.92%    |
| Epoch 50  |   65.89%   |    81.93%    |    87.62%    |
| Epoch 100 |   66.14%   |    87.36%    |    89.95%    |
| Epoch 200 |   68.80%   |    90.61%    |    93.58%    |

**训练阶段特征分析**：

- **FixMatch-4000** 呈"快速提升 → 缓慢精细调整"的两阶段模式。初始准确率 63.20%，得益于 4000 标签的良好初始化，前 50 epoch 提升至 87.62%，后续 200 epoch 精细调整至 94.49%。
- **FixMatch-250** 呈"S 形"学习曲线。前 15 epoch 快速提升至 76.46%，之后稳步提升。说明在 250 标签时模型需要一定的"预热期"来生成足够质量的伪标签。
- **FixMatch-40** 呈"早期跃升 → 长期平缓"模式。第 11 epoch 准确率从 26.55% 跃升至 37.77%（+11.22%），暗示伪标签质量在此阶段跨越了临界点。但第 100 epoch 后几乎停滞（66.14% → 68.99%），40 个标注样本提供的监督信号接近饱和。

### 4.3 损失函数分析

![损失函数对比](data/loss_comparison.png)

**图 4：有监督损失 $L_s$ 和无监督损失 $L_u$ 平滑曲线**

| 损失指标    | FixMatch-40 | FixMatch-250 | FixMatch-4000 |
| ----------- | :---------: | :----------: | :-----------: |
| 初始$L_s$ |   0.0434   |    0.2157    |    1.1142    |
| 最终$L_s$ |   0.0006   |    0.0005    |    0.0007    |
| 初始$L_u$ |   0.1479   |    0.1895    |    0.0468    |
| 最终$L_u$ |   0.0247   |    0.0221    |    0.0196    |

**$L_s$ 分析**：

- FixMatch-4000 初始 $L_s$ 最高（1.1142），因为每个 batch 包含更多类别的样本，模型需要同时拟合多样化的分类边界。FixMatch-40 初始 $L_s$ 仅 0.0434，因为小批量标注数据中类别多样性低。
- 三组实验的 $L_s$ 均收敛至极低水平，说明有监督分支已完全拟合标注数据。

**$L_u$ 分析**：

- FixMatch-4000 的 $L_u$ 初始值最低（0.0468），因为模型从较高准确率（63.20%）出发，生成的伪标签质量更好。
- FixMatch-40 的 $L_u$ 前 5 个 epoch 出现上升（0.1211 → 0.1739），反映早期伪标签质量差、模型在"探索"正确的决策方向。
- 最终 $L_u$ 排序：4000（0.0196）< 250（0.0221）< 40（0.0247），与准确率排序一致——更多标注 → 更好的伪标签质量 → 更低的无监督损失。

### 4.4 EMA 模型效果

![EMA 准确率](data/ema_accuracy_comparison.png)

**图 5：EMA 模型准确率平滑曲线**

| 指标             |   FixMatch-40   | FixMatch-250 |  FixMatch-4000  |
| ---------------- | :--------------: | :----------: | :--------------: |
| 原始模型最高 Acc |      69.10%      |    91.88%    |      94.49%      |
| EMA 模型最高 Acc |      69.55%      |    91.79%    |      94.62%      |
| EMA 增益         | **+0.45%** |   −0.09%   | **+0.13%** |

EMA 在低标注场景（40 labels）中增益最大（+0.45%），因为标注数据稀疏时参数波动更剧烈，EMA 的平滑作用更为关键。

### 4.5 训练稳定性分析

| 稳定性指标                | FixMatch-40 | FixMatch-250 | FixMatch-4000 |
| ------------------------- | :---------: | :----------: | :-----------: |
| 整体准确率标准差          |    14.42    |    12.56    |     8.00     |
| 后 50 Epoch 标准差        |    0.48    |     0.32     |     0.17     |
| 准确率"倒退"的 Epoch 比例 |    ~19%    |     ~14%     |     ~12%     |

标注量越少，训练越不稳定——少量标注数据使伪标签质量对当前模型状态高度敏感，容易形成"错误伪标签 → 错误训练 → 更差模型"的恶性循环。

---

## 5. 与 USB 库实现的对比

### 5.1 USB（原 TorchSSL）库简介

USB是微软发布的开源半监督学习基准库（原 TorchSSL 的升级维护版本），提供了 FixMatch、MixMatch、FlexMatch 等多种算法的统一实现。为验证自有实现的有效性，我使用 USB 库中的 FixMatch 在相同的 CIFAR-10 半监督设置下进行了完整的训练和测试。

USB 库的 FixMatch 与自有实现的主要差异在于：(1) 使用温度锐化 $T=0.5$、(2) 总训练步数为 $2^{20}=1{,}048{,}576$（自有实现为 262,144）、(3) 使用 FP32 精度（自有实现使用 AMP FP16）、(4) 使用 `use_cat` 拼接前传策略。详细配置文件见 `usb_official/config/classic_cv/fixmatch/`。

### 5.2 性能对比

在相同 CIFAR-10 数据集、相同 WideResNet-28-2 骨干网络下，两种实现的对比结果如下：

| 标注量 | 自有实现 (Best EMA) | USB FixMatch (Best EMA) |   差距   | USB 训练步数 |
| :----: | :-----------------: | :---------------------: | :------: | :----------: |
|   40   |       69.55%       |    **92.01%**    | −22.46% |  1,044,480  |
|  250  |       91.79%       |    **95.35%**    | −3.56% |  1,044,480  |
|  4000  |       94.62%       |    **95.83%**    | −1.21% |  1,044,480  |

![USB 最佳准确率柱状图](data/usb_best_comparison_bar.png)

**图 7：自有实现 vs USB 库最佳 EMA 准确率柱状图**

**性能差距随标注量增加而显著缩小**：40 标签差距最大（−22.46%），250 标签差距缩小至 −3.56%，4000 标签差距仅 −1.21%。这反映出标注数据越充足，训练迭代数和温度锐化等实现细节的影响越小。

### 5.3 差距原因分析

通过对比自有实现与 USB 的配置文件（`fixmatch_cifar10_40_0.yaml`）及训练日志，识别出以下关键差异：

| 差异项                   | 自有实现                        | USB 实现                            |    影响    |
| ------------------------ | ------------------------------- | ----------------------------------- | :--------: |
| **训练迭代数**     | ~262,144 步 (256×1024)         | **1,048,576 步** ($2^{20}$) | ★★★★★ |
| **温度锐化 $T$** | 无（直接 softmax，等价$T=1$） | **0.5**（sharpen）            |  ★★★★  |
| **use_cat**        | 否（分别前向传播）              | **是**（拼接前向传播）        |     ★     |
| **混合精度 AMP**   | 是 (FP16)                       | 否 (FP32)                           |     ★     |
| **评估频率**       | 每 epoch (1024步)               | 每 5120 步                          |     —     |

![USB 损失对比](data/usb_comparison_loss.png)

**图 8：自有实现与 USB 库的损失函数曲线对比**

**关键差异详解**：

#### (1) 训练迭代数 — 影响最大

USB 使用 $2^{20} = 1{,}048{,}576$ 步（FixMatch 论文标准设置），自有实现仅训练了约 $256 \times 1024 = 262{,}144$ 步，为 USB 的 **1/4**。从图 6 可以直观看到，自有实现在约 100 epoch（102,400 步）后准确率趋于饱和，而 USB 实现继续训练至约 1000 epoch 等效步数，性能持续提升。

在 40 标签场景中这一差距尤为显著：自有实现的 $L_u$ 最终停留在 0.0247，而 USB 的 $L_u$ 降至 0.0180——更低的 $L_u$ 意味着更好的伪标签质量和更强的一致性正则化效果。这也是 **40 标签场景下 22.46% 性能差距的最主要原因**。

#### (2) 温度锐化

USB 在生成伪标签时使用温度参数 $T=0.5$ 进行锐化：

$$
\text{sharpen}(q)_i = \frac{q_i^{1/T}}{\sum_j q_j^{1/T}}
$$

当 $T < 1$ 时，锐化操作放大了高置信度类别与低置信度类别之间的差距，使伪标签更加"尖锐"。这有助于：

- 增强高置信度样本的训练信号强度
- 加速模型从"不确定"到"确定"的过渡

本实验直接使用 softmax 概率生成伪标签（等价于 $T=1$），缺少锐化步骤，可能导致伪标签的区分度不足，特别是在低标注场景中。

#### (3) 温度锐化的定量影响

从损失曲线（图 8）可以观察到，USB 的 $L_u$ 在训练全程均低于自有实现，这在早期尤为明显。温度锐化 $T=0.5$ 使得伪标签更加"尖锐"，在置信度阈值 $\tau=0.95$ 不变的情况下，更多样本可以达到阈值标准，从而加速了伪标签质量的提升。

#### (4) 混合精度训练

本实验使用 AMP（FP16 混合精度）训练，USB 使用标准 FP32。AMP 在大部分情况下对准确率影响极小（通常 < 0.1%），且能显著加速训练，因此不是一个主要的影响因素。

### 5.4 改进方向

基于以上分析，提出以下改进方向以缩小与 USB 的性能差距：

1. **增加训练迭代数至 1M 步**：这是最直接且最有效的改进。将 `epochs` 增加至约 1024（保持 1024 steps/epoch），使总训练步数达到 $2^{20}$ 步。
2. **引入温度锐化**：在伪标签生成时加入温度参数 $T=0.5$：

   ```python
   probs = torch.softmax(logits_u_w / T, dim=1)  # 温度锐化
   max_probs, pseudo_labels = torch.max(probs, dim=1)
   ```
3. **多种子运行**：使用 3 个不同随机种子（如 0, 1, 2）运行实验，报告均值 ± 标准差。
4. **使用 `use_cat` 提升效率**：将三个输入拼接后一次前向传播，减少显存碎片并加速训练。

---

## 6. FixMatch 与其他半监督算法的比较

### 6.1 半监督学习算法演进

半监督深度学习经历了从早期一致性正则化方法到现代混合方法的演进：

| 方法                   | 年份 | 核心思想                       | 增强策略                      |  伪标签  |
| ---------------------- | :--: | ------------------------------ | ----------------------------- | :------: |
| Π-Model [2]           | 2017 | 同一图像两次前传输出一致       | 基础增强 + Dropout            |    无    |
| Mean Teacher [3]       | 2017 | 教师模型监督学生模型           | 基础增强 + EMA                |    无    |
| **MixMatch** [4] | 2019 | 一致性 + 熵最小化 + MixUp      | K 次弱增强取平均              | 温度锐化 |
| **FixMatch** [5] | 2020 | 弱增强→伪标签，强增强→一致性 | 弱增强 + 强增强 (RandAugment) | 阈值过滤 |

### 6.2 FixMatch 与 MixMatch 详细对比

MixMatch 和 FixMatch 是半监督学习中两个具有代表性的方法，FixMatch 可视为 MixMatch 的"简化+增强"版本。

#### 6.2.1 算法流程对比

**MixMatch 的完整流程**（两步混合策略）：

```
输入: 有标签 batch X, 无标签 batch U
1. 对每个无标签图像 u，生成 K 次弱增强版本（K=2）
2. 对 K 个版本的预测取平均: q̄ = avg(softmax(model(weak_aug(u))))
3. 温度锐化: sharpen(q̄) = q̄^(1/T) / sum(q̄^(1/T))
4. 将有标签 X 和无标签 U（使用锐化伪标签）混合打乱
5. 对整个混合 batch 施加 MixUp（β分布采样 λ，线性插值图像和标签）
6. 对 MixUp 后的有标签部分使用交叉熵损失 L_s
7. 对 MixUp 后的无标签部分使用 MSE 损失 L_u (L2 距离)
8. L_u 的权重从 0 线性 warm-up 至 λ_u
```

**FixMatch 的极简流程**（单步阈值策略）：

```
输入: 有标签 batch X, 无标签 batch U
1. 对每个无标签图像 u，生成 1 次弱增强（预测伪标签）
2. 直接使用 softmax 概率（无温度锐化，无 K 次平均）
3. 仅保留 max(prob) >= 0.95 的高置信度伪标签
4. 对同一样本施加强增强（RandAugment）
5. 用伪标签以交叉熵损失监督强增强版本的输出
6. 总损失 = L_s + L_u（无需 warm-up，固定权重）
```

#### 6.2.2 关键差异总结

| 维度                 | MixMatch                   | FixMatch                                  |
| -------------------- | -------------------------- | ----------------------------------------- |
| **伪标签生成** | K 次增强平均 + 温度锐化    | 单次弱增强 + 直接 softmax                 |
| **置信度策略** | 所有样本参与训练（无过滤） | 高阈值过滤（$\tau=0.95$），低置信度丢弃 |
| **数据增强**   | K 次弱增强 + MixUp         | 弱增强 vs 强增强（RandAugment）           |
| **无监督损失** | MSE（L2 距离）             | 交叉熵（更尖锐的监督信号）                |
| **损失权重**   | 线性 warm-up               | 固定权重$\lambda_u=1$                   |
| **计算复杂度** | 较高（K 次前传 + MixUp）   | 较低（2 次前传，无需 MixUp）              |
| **超参数数量** | 较多（T, α, K, warm-up）  | 较少（$\tau$, $\mu$）                 |

#### 6.2.3 设计哲学差异

**MixMatch** 的设计哲学是"混合一切"（Holistic）：

- 将一致性正则化、熵最小化、MixUp 正则化、温度锐化等多种技术统一融合。
- 通过 MixUp 在标注和无标注样本之间建立平滑插值，创造"虚拟训练样本"，增强模型的插值泛化能力。
- 所有无标签样本都参与训练（K 次平均缓解伪标签噪声），用 MSE 做软监督。

**FixMatch** 的设计哲学是"少即是多"（Simplification）：

- 去掉了 MixMatch 中的 K 次平均、温度锐化、MixUp 和损失 warm-up，仅保留伪标签 + 一致性正则化的最简形式。
- 用更激进的方法保证无监督信号质量：不做 K 次平均、不做软标签，而是**直接丢弃**低置信度样本（"宁愿不用，也不要用错"）。
- 用**强增强**（RandAugment）替代 MixUp 来提供正则化，强增强比 MixUp 更简单且效果更好。
- 从 MixMatch 到 FixMatch 的简化说明：**在半监督学习中，"如何选择可靠的无监督信号"比"如何融合多种正则化技术"更重要**。

#### 6.2.4 性能对比

使用 USB 库在同一框架下运行 FixMatch 和 MixMatch 的结果如下（`classic_cv.csv` 存储的数据）：

| 方法                            | CIFAR-10 (40 labels) | CIFAR-10 (250 labels) | CIFAR-10 (4000 labels) |
| ------------------------------- | :------------------: | :-------------------: | :--------------------: |
| MixMatch                        |        29.33%        |        62.72%        |         92.62%         |
| **FixMatch**（USB库运行） |   **92.01%**   |   **95.35%**   |    **95.83%**    |
| 全监督                          |        95.40%        |        95.45%        |         95.42%         |

**关键观察**：

- **FixMatch 在所有标注量级别上全面优于 MixMatch**。最显著的差距在 40 标签：FixMatch（92.01%）vs MixMatch（29.33%），差距高达 **62.68%**。
- MixMatch 在 40 标签场景下几乎失效，因为低标注场景下 K 次弱增强平均仍不足以产生可靠的伪标签，而 MixUp 混合了错误伪标签反而"污染"了有监督训练。
- **FixMatch 的置信度阈值策略在低标注场景中展现了决定性优势**：宁愿丢弃 95% 的无标签数据，也要保证剩余 5% 的伪标签是可靠的。
- 在 4000 标签时两者差距缩小，因为足够的标注数据本身就保证了伪标签质量，阈值过滤的优势减弱。

#### 6.2.5 MixMatch 与 FixMatch 的适用场景

| 场景                          | 推荐方法           | 原因                                    |
| ----------------------------- | ------------------ | --------------------------------------- |
| 极度稀缺标注 (< 100 labels)   | **FixMatch** | 置信度阈值过滤能有效排除噪声伪标签      |
| 标注量适中 (100–1000 labels) | **FixMatch** | 简洁高效，超参数少                      |
| 标注量充足 (> 1000 labels)    | 两者皆可           | 伪标签质量高，差异缩小                  |
| 计算资源受限                  | **FixMatch** | 无需 K 次前传和 MixUp，训练更快         |
| 需要软标签/不确定性估计       | MixMatch           | MSE 损失 + 温度锐化提供更丰富的概率信息 |

---

## 7. 结论

### 7.1 实验总结

本实验基于 PyTorch 手动实现了 FixMatch 半监督图像分类算法，在 CIFAR-10 数据集上评估了三种标注量（40/250/4000）下的分类性能，并与 USB 库实现及 MixMatch 算法进行了对比分析。主要结论如下：

1. **FixMatch 是高效的半监督学习方法**：仅需 40 张标签即可达到 69.55% 的准确率，250 张标签可达 91.79%，4000 张标签可达 94.62%，接近全监督。这验证了"弱增强伪标签 + 强增强一致性正则化 + 高置信度过滤"三件套的有效性。
2. **与 USB 库实现的差距主要由训练迭代数不足和缺少温度锐化导致**：自有实现训练 262K 步，USB 训练 1,048K 步（4 倍差距），USB 额外使用温度锐化 $T=0.5$。在 40 标签场景中，这两项差异共同导致了 22.46% 的性能差距；而在 4000 标签场景中差距缩至仅 1.21%，说明标注充足时实现细节的影响大幅减弱。
3. **FixMatch 在低标注场景中显著优于 MixMatch**：USB 运行结果中，40 标签下 FixMatch（92.01%）vs MixMatch（29.33%），差距 62.68%，置信度阈值过滤是低标注场景的关键。
4. **标注数据量的边际收益递减**：40→250 标签提升 +22.24%，250→4000 仅提升 +2.83%。250 标签（每类 25 张）是 CIFAR-10 上半监督学习的高性价比标注预算。
5. **EMA 和强增强是低标注场景的关键组件**：EMA 在 40 标签场景带来 +0.45% 的额外提升，RandAugment 强增强使一致性正则化更加有效。

### 7.2 改进方向

- 增加训练迭代至 $2^{20}$ 步，预计可显著提升 40 标签场景性能。
- 引入温度锐化（$T=0.5$）增强伪标签区分度。
- 使用多随机种子运行以提升统计可靠性。
- 探索动态置信度阈值（如 FlexMatch 的课程式阈值策略）以加速低标注场景收敛。

---

## 8. 参考文献

[1] Y. Ouali, C. Hudelot, and M. Tami, "A Survey on Deep Semi-supervised Learning," *arXiv:2103.00550*, 2021.

[2] S. Laine and T. Aila, "Temporal Ensembling for Semi-Supervised Learning," *ICLR 2017*.

[3] A. Tarvainen and H. Valpola, "Mean Teachers are Better Role Models: Weight-Averaged Consistency Targets Improve Semi-Supervised Deep Learning Results," *NeurIPS 2017*.

[4] D. Berthelot, N. Carlini, I. Goodfellow, N. Papernot, A. Oliver, and C. Raffel, "MixMatch: A Holistic Approach to Semi-Supervised Learning," *NeurIPS 2019*.

[5] K. Sohn, D. Berthelot, N. Carlini, Z. Zhang, H. Zhang, C. Raffel, E. D. Cubuk, A. Kurakin, and C.-L. Li, "FixMatch: Simplifying Semi-Supervised Learning with Consistency and Confidence," *NeurIPS 2020*.

[6] E. D. Cubuk, B. Zoph, J. Shlens, and Q. V. Le, "RandAugment: Practical Automated Data Augmentation with a Reduced Search Space," *NeurIPS 2020*.

[7] S. Zagoruyko and N. Komodakis, "Wide Residual Networks," *BMVC 2016*.

[8] USB (Unified Semi-supervised Learning Benchmark): [https://github.com/microsoft/Semi-supervised-learning](https://github.com/microsoft/Semi-supervised-learning)

---

## 附录 A. 项目文件结构

```
FixMatch_Project/
├── config.py              # 超参数配置（Config40/Config250/Config4000）
├── train.py               # FixMatch 训练主程序（含 EMA 和混合精度）
├── dataset.py             # 数据划分与加载（CIFAR10Labeled/Unlabeled）
├── utils/
│   └── augment.py         # 弱/强数据增强变换
├── models/
│   └── wrn.py             # WideResNet-28-2 模型
├── data/
│   ├── fixmatch_40.csv          # 40 标签实验日志（自有实现）
│   ├── fixmatch_250.csv         # 250 标签实验日志（自有实现）
│   ├── fixmatch_4000.csv        # 4000 标签实验日志（自有实现）
│   ├── usb_fixmatch_40.csv      # 40 标签实验日志（USB 库运行）
│   ├── usb_fixmatch_250.csv     # 250 标签实验日志（USB 库运行）
│   ├── usb_fixmatch_4000.csv    # 4000 标签实验日志（USB 库运行）
│   └── *.png                    # 实验图表
├── plot_results.py              # 自有实验结果可视化
├── plot_usb_comparison.py       # USB 对比可视化
├── usb_official/                # USB 库源码及配置
└── 实验报告.md            # 本报告
```

## 附录 B. 图表清单

| 文件                                 | 内容                             |
| ------------------------------------ | -------------------------------- |
| `data/best_accuracy_bar.png`       | 三种标注配置的最佳准确率柱状图   |
| `data/accuracy_comparison.png`     | 测试准确率平滑曲线对比           |
| `data/ema_accuracy_comparison.png` | EMA 准确率曲线对比               |
| `data/loss_comparison.png`         | 有监督/无监督损失曲线对比        |
| `data/lr_schedule.png`             | 学习率衰减曲线                   |
| `data/full_comparison.png`         | 四合一综合对比大图               |
| `data/usb_comparison_accuracy.png` | 自有实现 vs USB 准确率三行对比   |
| `data/usb_comparison_loss.png`     | 自有实现 vs USB 损失函数三行对比 |
| `data/usb_best_comparison_bar.png` | 自有实现 vs USB 最佳准确率柱状图 |
| `data/usb_full_comparison.png`     | 自有实现 vs USB 综合对比大图     |
